import styled from 'styled-components';

export const TagContainer = styled.div`
  padding: 10px 15px;
  background-color: #ebeff2;
  border-radius: 20px;
  width: fit-content;

  :hover {
    cursor: pointer;
  }
`;